using Gtk 4.0;
using Adw 1;

template $LibellusPage: Adw.Bin {
  child: Gtk.ScrolledWindow {
    child: Gtk.Box box {
      margin-start: 10;
      margin-end: 10;
      margin-top: 10;
      margin-bottom: 10;
      spacing: 10;
      orientation: vertical;
    };
  };
}